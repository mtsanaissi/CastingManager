<Page
    x:Class="CastingManager.UWP.MainPage"
    x:Name="page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CastingManager.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CastingManager.UWP.Models"
    xmlns:converters="using:CastingManager.UWP.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12" Spacing="8">
            <TextBlock Text="Casting Devices" FontSize="20" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="StartCastingButton" Content="Start Casting" Click="StartCastingButton_Click"/>
                <Button x:Name="StartDiscoveryButton" Content="Start Discovery" Click="StartDiscoveryButton_Click"/>
                <Button x:Name="StopDiscoveryButton" Content="Stop Discovery" Click="StopDiscoveryButton_Click"/>
            </StackPanel>
            <TextBlock x:Name="DeviceSummaryText"
                       Text="Devices: (debug info pending)"
                       FontStyle="Italic"
                       Foreground="Gray"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="12"
                Background="{ThemeResource SystemControlAcrylicWindowBrush}"
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Device list region"
                           Foreground="Gray"
                           FontStyle="Italic"
                           Margin="0,0,0,8"/>

                <ListView x:Name="DeviceListView"
                          Grid.Row="1"
                          ItemsSource="{x:Bind Devices, Mode=OneWay}"
                          Loaded="DeviceListView_Loaded">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:CastingDeviceModel">
                            <StackPanel Margin="6">
                                <TextBlock Text="{Binding DeviceName}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding DeviceType}"/>
                                <TextBlock Text="{Binding StatusText}" Margin="0,4,0,0"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Content="Connect"
                                            Tag="{Binding}"
                                            IsEnabled="{Binding IsConnectable}"
                                            Visibility="{Binding IsConnected, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                            Click="ConnectButton_Click"/>
                                    <Button Content="Disconnect"
                                            Tag="{Binding}"
                                            Visibility="{Binding CanDisconnect, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Click="DisconnectButton_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock x:Name="EmptyStateText"
                           Grid.Row="1"
                           Text="No devices yet. Waiting for discovery..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Page>
