<Page
    x:Class="CastingManager.UWP.MainPage"
    x:Name="page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CastingManager.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CastingManager.UWP.Models"
    xmlns:converters="using:CastingManager.UWP.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12" Orientation="Horizontal">
            <Button x:Name="StartCastingButton" Content="Start Casting" Click="StartCastingButton_Click"/>
            <Button x:Name="StartDiscoveryButton" Content="Start Discovery" Margin="12,0,0,0" Click="StartDiscoveryButton_Click"/>
            <Button x:Name="StopDiscoveryButton" Content="Stop Discovery" Margin="12,0,0,0" Click="StopDiscoveryButton_Click"/>
        </StackPanel>

        <ListView x:Name="DeviceListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CastingDeviceModel">
                    <StackPanel Margin="6">
                        <TextBlock Text="{Binding DeviceName}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding DeviceType}"/>
                        <TextBlock Text="{Binding ConnectionStatus}"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Connect" Tag="{Binding}" Visibility="{Binding IsConnected, Converter={StaticResource InverseBoolToVisibilityConverter}}" Click="ConnectButton_Click"/>
                            <Button Content="Disconnect" Tag="{Binding}" Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}" Click="DisconnectButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
